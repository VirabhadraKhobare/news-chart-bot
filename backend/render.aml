services:
  - type: web
    name: news-chart-bot-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NEWS_API_KEY
        value: your_api_key_here
